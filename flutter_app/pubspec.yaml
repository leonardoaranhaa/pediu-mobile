name: pediu_flutter
description: Cliente Flutter nativo do Pediu integrado ao backend Express/tRPC.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  app_links: ^6.4.0
  flutter_secure_storage: ^9.2.4
  http: ^1.3.0
  shared_preferences: ^2.5.3
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
