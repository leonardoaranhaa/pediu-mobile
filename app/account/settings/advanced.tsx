import { useState } from "react";
import { Text } from "react-native";
import { Page, Card, ToggleRow, OutlineButton, PEDIU, s } from "@/components/pediu-page";
export default function AdvancedSettingsPage(){const [analytics,setAnalytics]=useState(true);const [location,setLocation]=useState(false);const [diagnostics,setDiagnostics]=useState(false);return <Page title="Configurações avançadas" eyebrow="CONTROLE"><Card><ToggleRow icon="analytics" title="Dados de uso" subtitle="Permitir métricas anônimas para melhorar o app" value={analytics} onChange={setAnalytics}/><ToggleRow icon="location-on" title="Localização" subtitle="Usar localização para contexto de entrega" value={location} onChange={setLocation}/><ToggleRow icon="bug-report" title="Diagnóstico" subtitle="Enviar registros técnicos quando houver erro" value={diagnostics} onChange={setDiagnostics}/></Card><Card><Text style={{fontSize:14,fontWeight:"900",color:PEDIU.ink}}>Ambiente</Text><Text style={s.muted}>Essas opções controlam o comportamento local do aplicativo nesta etapa. Persistência remota e políticas definitivas entram em uma etapa posterior.</Text><OutlineButton title="Limpar preferências locais" onPress={()=>{setAnalytics(true);setLocation(false);setDiagnostics(false)}}/></Card></Page>}