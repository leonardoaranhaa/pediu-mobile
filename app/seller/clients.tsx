import { Text } from "react-native"; import { useAuth } from "@/hooks/use-auth"; import { trpc } from "@/lib/trpc"; import { Page, Card, Row, s } from "@/components/pediu-page";
export default function SellerClientsPage(){const {user}=useAuth();const q=trpc.pediu.clients.mine.useQuery(undefined,{enabled:user?.role==="merchant"});return <Page title="Clientes e fiado" eyebrow="RELACIONAMENTO"><Card>{q.data?.length?q.data.map(c=><Row key={c.id} icon="person" title={c.name} subtitle={`Crédito: R$ ${Number(c.creditLimit).toFixed(2).replace(".",",")} · Saldo: R$ ${Number(c.balance).toFixed(2).replace(".",",")}`}/>):<Text style={s.muted}>Nenhum cliente cadastrado.</Text>}</Card></Page>}